A simple randomized screensaver, has various screensaver and on start chooses a random one

## Building
To build the app all you need to do is run the build.sh script
```
./build.sh
# if this doesn't work try running "chmod +x build.sh" first
```